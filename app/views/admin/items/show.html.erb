<div class="container">
  <div class="row">
    <div class="col-sm-11">
      <h1 class="text-left">商品詳細</h1>
      <div class="row">
        
        <!-- 左側 -->
        <div class="col-sm-3">
          <div class="product-details">
            <div class="product-image">
              <%= image_tag @item.image, size: "300x300", class: "img-fluid" %>
            </div>
          </div>
        </div>
        
        <!-- 右側 -->
        <div class="col-sm-9">
          <table class="table table-bordered">
            <tbody>
              <tr>
                <td>商品名</td>
                <td><%= @item.name %></td>
              </tr>
              <tr>
                <td>商品説明</td>
                <td><%= @item.introduction %></td>
              </tr>
              <tr>
                <td>ジャンル</td>
                <td><%= @item.genre.name %></td>
              </tr>
              <tr>
                <td>価格</td>
                <td>税抜価格: <%= number_with_delimiter(@item.price, delimiter: ',') %> 円</td>
              </tr>
              <tr>
                <td>ステータス</td>
                <td class="<%= @item.is_active ? 'text-success' : 'text-danger' %>">
                  <% if @item.is_active == true %>
                    販売中
                  <% else %>
                    販売停止中
                  <% end %>
                </td>
              </tr>
            </tbody>
          </table>
        </div>
        
        
        <div class="text-center">
          <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success" %>
          <%= link_to "削除する", admin_item_path(@item), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger" %>
        </div>
      </div>
    </div>
  </div>
</div>