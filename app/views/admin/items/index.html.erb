<h1>商品一覧</h1>
<table>
  <tr>
    <th>商品名</th>
    <th>価格</th>
  </tr>
  <% @items.each do |item| %>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.price %>円</td>
    </tr>
  <% end %>
</table>