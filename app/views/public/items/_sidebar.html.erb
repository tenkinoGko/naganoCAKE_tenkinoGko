<div class="sidebar">
  <h4>ジャンル検索</h4>
  <table>
    <% @genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.name, genre_path(genre) %>
        </td>
      </tr>
    <% end %>
    <tr>
      <td>
        <%= link_to ">>すべての商品を見る", items_path %>
      </td>
    </tr>
  </table>
</div>